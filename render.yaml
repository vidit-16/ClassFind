services:
  - type: web
    name: classfind
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: classfind-db
          property: connectionString

databases:
  - name: classfind-db
